export const My_named_Component = () => {
    return <h1>We will be learning Export and Import here</h1>;
};

export const Another_named_Component = () => {
    return <p2>We got a cheatsheet in node which includes git status, git add, git commit, git push, git checkout, git switch and npm start  </p2>;
};