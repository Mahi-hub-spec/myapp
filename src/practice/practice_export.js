import React from "react";

const default_Component = () => {
    return
     
     <p2>We got a cheatsheet in node which includes git status, git add, git commit, git push, git checkout, git switch and npm start  </p2>;
};

export default default_Component;